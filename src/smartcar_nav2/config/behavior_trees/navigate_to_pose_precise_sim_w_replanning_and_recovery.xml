<!-- Gazebo P-to-A navigation: native Nav2 planning and RPP only. -->
<root main_tree_to_execute="MainTree">
  <BehaviorTree ID="MainTree">
    <RecoveryNode number_of_retries="1" name="NavigatePreciseSimRecovery">
      <PipelineSequence name="NavigatePreciseSimWithReplanning">
        <RateController hz="1.0">
          <RecoveryNode number_of_retries="1" name="ComputePreciseSimPath">
            <ComputePathToPose
                goal="{goal}"
                path="{path}"
                planner_id="GridBased"/>
            <ClearEntireCostmap
                name="ClearGlobalCostmap-PreciseSimContext"
                service_name="global_costmap/clear_entirely_global_costmap"/>
          </RecoveryNode>
        </RateController>
        <!-- A collision must reach the outer recovery immediately. Retrying
             FollowPath after only a local clear re-sends the stale {path}
             and consumes the controller patience window before GridBased can
             produce a fresh route. The outer branch clears both costmaps and
             re-enters this PipelineSequence at ComputePathToPose. -->
        <FollowPath
            path="{path}"
            controller_id="FollowPath"
            goal_checker_id="precise_goal_checker"/>
      </PipelineSequence>
      <Sequence name="ClearPreciseSimCostmapsRecovery">
        <ClearEntireCostmap
            name="ClearLocalCostmap-PreciseSimSubtree"
            service_name="local_costmap/clear_entirely_local_costmap"/>
        <ClearEntireCostmap
            name="ClearGlobalCostmap-PreciseSimSubtree"
            service_name="global_costmap/clear_entirely_global_costmap"/>
      </Sequence>
    </RecoveryNode>
  </BehaviorTree>
</root>
