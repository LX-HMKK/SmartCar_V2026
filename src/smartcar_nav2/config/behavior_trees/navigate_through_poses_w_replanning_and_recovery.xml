<!-- Ackermann-safe tree: no backup, no wait, no spin.  Clear costmaps + replan only. -->
<root main_tree_to_execute="MainTree">
  <BehaviorTree ID="MainTree">
    <RecoveryNode number_of_retries="5" name="NavigateRecovery">
      <PipelineSequence name="NavigateWithReplanning">
        <!-- Keep the active goal list fresh on every tick. The full route
             stays latched while FollowPath runs so a long candidate search
             cannot publish a stale-start path. -->
        <!-- Transit constraints are pass-through gates, not stop points. -->
        <RemovePassedGoals input_goals="{goals}" output_goals="{goals}" radius="0.50" global_frame="odom_combined" robot_base_frame="base_footprint"/>
        <LatchSuccess name="LatchFreeHeadingPathThroughPoses">
          <RecoveryNode number_of_retries="1" name="ComputeFreeHeadingPathThroughPoses">
            <ComputeFreeHeadingPathThroughPoses
                goals="{goals}"
                path="{path}"
                planner_id="GridBased"
                heading_samples="12"
                candidate_timeout_ms="1000"
                search_budget_ms="2400"
                through_search_budget_ms="12000"
                fallback_candidate_limit="4"
                lookahead_fallback_candidate_limit="0"
                through_solution_limit="4"
                max_start_drift_m="0.10"
                footprint_half_length_m="0.2491"
                    footprint_half_width_m="0.095"
                    footprint_sweep_step_m="0.025"
                    footprint_lethal_cost="253"
                    local_tracking_cross_track_error_m="0.10"
                    local_tracking_horizon_m="0.50"
                    local_tracking_lethal_cost="254"/>
            <ClearEntireCostmap name="ClearGlobalCostmap-Context" service_name="global_costmap/clear_entirely_global_costmap"/>
          </RecoveryNode>
        </LatchSuccess>
        <RecoveryNode number_of_retries="1" name="FollowPath">
          <RecordFollowPath path="{path}" controller_id="ForwardAvoidance" goal_checker_id="goal_checker"/>
          <ClearEntireCostmap name="ClearLocalCostmap-Context" service_name="local_costmap/clear_entirely_local_costmap"/>
        </RecoveryNode>
      </PipelineSequence>
      <Sequence name="ClearCostmapsRecovery">
        <ClearEntireCostmap name="ClearLocalCostmap-Subtree" service_name="local_costmap/clear_entirely_local_costmap"/>
        <ClearEntireCostmap name="ClearGlobalCostmap-Subtree" service_name="global_costmap/clear_entirely_global_costmap"/>
      </Sequence>
    </RecoveryNode>
  </BehaviorTree>
</root>
