bt_navigator:
  ros__parameters:
    use_sim_time: false
    global_frame: odom_combined
    robot_base_frame: base_footprint
    odom_topic: /odom_combined
    # Milliseconds. 20 ms halves the default 100 Hz BT load while remaining
    # faster than the independent 20 Hz path-controller loop.
    bt_loop_duration: 20
    # Nav2 interprets this value in milliseconds.  Ten milliseconds is below
    # the observed local-costmap clear round trip in the simulator, which made
    # a legitimate collision recovery abort before it could replan.
    default_server_timeout: 100
    default_nav_to_pose_bt_xml: "@SMARTCAR_NAV2_SHARE_DIR@/config/behavior_trees/navigate_to_pose_w_replanning_and_recovery.xml"
    default_nav_through_poses_bt_xml: "@SMARTCAR_NAV2_SHARE_DIR@/config/behavior_trees/navigate_through_poses_w_replanning_and_recovery.xml"
    # Setting plugin_lib_names replaces Nav2's defaults, so retain the full
    # 1.1.20 list and append the project reverse-path node.
    plugin_lib_names:
      - nav2_compute_path_to_pose_action_bt_node
      - nav2_compute_path_through_poses_action_bt_node
      - nav2_compute_and_track_route_bt_node
      - nav2_compute_route_bt_node
      - nav2_smooth_path_action_bt_node
      - nav2_follow_path_action_bt_node
      - nav2_spin_action_bt_node
      - nav2_wait_action_bt_node
      - nav2_assisted_teleop_action_bt_node
      - nav2_back_up_action_bt_node
      - nav2_drive_on_heading_bt_node
      - nav2_clear_costmap_service_bt_node
      - nav2_is_stuck_condition_bt_node
      - nav2_goal_reached_condition_bt_node
      - nav2_initial_pose_received_condition_bt_node
      - nav2_goal_updated_condition_bt_node
      - nav2_globally_updated_goal_condition_bt_node
      - nav2_is_path_valid_condition_bt_node
      - nav2_reinitialize_global_localization_service_bt_node
      - nav2_rate_controller_bt_node
      - nav2_distance_controller_bt_node
      - nav2_speed_controller_bt_node
      - nav2_truncate_path_action_bt_node
      - nav2_truncate_path_local_action_bt_node
      - nav2_get_pose_from_path_action_bt_node
      - nav2_goal_updater_node_bt_node
      - nav2_recovery_node_bt_node
      - nav2_pipeline_sequence_bt_node
      - nav2_round_robin_node_bt_node
      - nav2_transform_available_condition_bt_node
      - nav2_time_expired_condition_bt_node
      - nav2_path_expiring_timer_condition
      - nav2_distance_traveled_condition_bt_node
      - nav2_single_trigger_bt_node
      - nav2_goal_updated_controller_bt_node
      - nav2_is_battery_low_condition_bt_node
      - nav2_navigate_through_poses_action_bt_node
      - nav2_navigate_to_pose_action_bt_node
      - nav2_remove_passed_goals_action_bt_node
      - nav2_planner_selector_bt_node
      - nav2_controller_selector_bt_node
      - nav2_goal_checker_selector_bt_node
      - nav2_progress_checker_selector_bt_node
      - nav2_smoother_selector_bt_node
      - nav2_controller_cancel_bt_node
      - nav2_path_longer_on_approach_bt_node
      - nav2_wait_cancel_bt_node
      - nav2_spin_cancel_bt_node
      - nav2_assisted_teleop_cancel_bt_node
      - nav2_back_up_cancel_bt_node
      - nav2_drive_on_heading_cancel_bt_node
      - nav2_is_battery_charging_condition_bt_node
      - smartcar_compute_reverse_path_to_pose_action_bt_node
      - smartcar_compute_reverse_path_through_poses_action_bt_node
      - smartcar_compute_free_heading_path_action_bt_node
      - smartcar_ackermann_reverse_retreat_action_bt_node

bt_navigator_navigate_through_poses_rclcpp_node:
  ros__parameters:
    use_sim_time: false
    global_frame: odom_combined
    robot_base_frame: base_footprint
    odom_topic: /odom_combined
    bt_loop_duration: 20
    # Keep the ThroughPoses navigator aligned with the primary navigator.
    default_server_timeout: 100
    default_nav_to_pose_bt_xml: "@SMARTCAR_NAV2_SHARE_DIR@/config/behavior_trees/navigate_to_pose_w_replanning_and_recovery.xml"
    default_nav_through_poses_bt_xml: "@SMARTCAR_NAV2_SHARE_DIR@/config/behavior_trees/navigate_through_poses_w_replanning_and_recovery.xml"
    # Same plugin list as primary bt_navigator; through-poses BT XML uses a subset.
    plugin_lib_names:
      - nav2_compute_path_to_pose_action_bt_node
      - nav2_compute_path_through_poses_action_bt_node
      - nav2_compute_and_track_route_bt_node
      - nav2_compute_route_bt_node
      - nav2_smooth_path_action_bt_node
      - nav2_follow_path_action_bt_node
      - nav2_spin_action_bt_node
      - nav2_wait_action_bt_node
      - nav2_assisted_teleop_action_bt_node
      - nav2_back_up_action_bt_node
      - nav2_drive_on_heading_bt_node
      - nav2_clear_costmap_service_bt_node
      - nav2_is_stuck_condition_bt_node
      - nav2_goal_reached_condition_bt_node
      - nav2_initial_pose_received_condition_bt_node
      - nav2_goal_updated_condition_bt_node
      - nav2_globally_updated_goal_condition_bt_node
      - nav2_is_path_valid_condition_bt_node
      - nav2_reinitialize_global_localization_service_bt_node
      - nav2_rate_controller_bt_node
      - nav2_distance_controller_bt_node
      - nav2_speed_controller_bt_node
      - nav2_truncate_path_action_bt_node
      - nav2_truncate_path_local_action_bt_node
      - nav2_get_pose_from_path_action_bt_node
      - nav2_goal_updater_node_bt_node
      - nav2_recovery_node_bt_node
      - nav2_pipeline_sequence_bt_node
      - nav2_round_robin_node_bt_node
      - nav2_transform_available_condition_bt_node
      - nav2_time_expired_condition_bt_node
      - nav2_path_expiring_timer_condition
      - nav2_distance_traveled_condition_bt_node
      - nav2_single_trigger_bt_node
      - nav2_goal_updated_controller_bt_node
      - nav2_is_battery_low_condition_bt_node
      - nav2_navigate_through_poses_action_bt_node
      - nav2_navigate_to_pose_action_bt_node
      - nav2_remove_passed_goals_action_bt_node
      - nav2_planner_selector_bt_node
      - nav2_controller_selector_bt_node
      - nav2_goal_checker_selector_bt_node
      - smartcar_compute_free_heading_path_action_bt_node
      - smartcar_ackermann_reverse_retreat_action_bt_node

bt_navigator_navigate_to_pose_rclcpp_node:
  ros__parameters:
    use_sim_time: false

controller_server:
  ros__parameters:
    use_sim_time: false
    controller_frequency: 20.0
    min_x_velocity_threshold: 0.001
    min_y_velocity_threshold: 0.5
    min_theta_velocity_threshold: 0.001
    failure_tolerance: 0.3
    progress_checker_plugin: "progress_checker"
    goal_checker_plugins: ["goal_checker", "precise_goal_checker", "reverse_goal_checker", "transit_goal_checker", "recovery_goal_checker"]
    current_goal_checker: "goal_checker"
    controller_plugins: ["FollowPath", "ReverseHandoff", "ReverseRecovery"]
    progress_checker:
      plugin: "nav2_controller::SimpleProgressChecker"
      required_movement_radius: 0.25
      movement_time_allowance: 15.0
    goal_checker:
      plugin: "nav2_controller::SimpleGoalChecker"
      xy_goal_tolerance: 0.35
      yaw_goal_tolerance: 0.50
      stateful: false
    # The QR pose seeds a direction change. This smaller envelope stays clear
    # of the long-CCC handoff region found in the offline Dubins sampling.
    precise_goal_checker:
      plugin: "nav2_controller::SimpleGoalChecker"
      xy_goal_tolerance: 0.12
      yaw_goal_tolerance: 0.15
      stateful: false
    reverse_goal_checker:
      plugin: "nav2_controller::SimpleGoalChecker"
      xy_goal_tolerance: 0.30
      yaw_goal_tolerance: 0.25
      stateful: false
    # Non-task route points are position constraints.  Requiring an arrival
    # yaw here makes an Ackermann vehicle overshoot a valid transit point
    # while trying to remove a heading error it was never asked to satisfy.
    transit_goal_checker:
      plugin: "nav2_controller::PositionGoalChecker"
      # Transit gates are intentionally coarse while route geometry is being
      # verified. Task poses retain their own precise/yaw-aware checkers.
      xy_goal_tolerance: 0.50
      stateful: false
    # A recovery path is only 0.15 m, so it cannot share the wide transit
    # tolerance or it would finish before issuing a physical reverse command.
    recovery_goal_checker:
      plugin: "nav2_controller::SimpleGoalChecker"
      xy_goal_tolerance: 0.03
      yaw_goal_tolerance: 0.10
      stateful: false
    FollowPath:
      plugin: "nav2_regulated_pure_pursuit_controller::RegulatedPurePursuitController"
      desired_linear_vel: 0.15
      lookahead_dist: 0.8
      min_lookahead_dist: 0.5
      max_lookahead_dist: 1.2
      lookahead_time: 1.5
      transform_tolerance: 0.1
      use_velocity_scaled_lookahead_dist: true
      min_approach_linear_velocity: 0.05
      approach_velocity_scaling_dist: 0.6
      max_allowed_time_to_collision_up_to_carrot: 1.0
      use_collision_detection: true
      use_cost_regulated_linear_velocity_scaling: true
      cost_scaling_dist: 0.25
      cost_scaling_gain: 1.0
      inflation_cost_scaling_factor: 5.0
      regulated_linear_scaling_min_radius: 0.9
      regulated_linear_scaling_min_speed: 0.05
      use_rotate_to_heading: false
      allow_reversing: true
    # The reverse-to-forward handoff needs closed-loop terminal pose control.
    # The wrapper maps the real reverse path and odometry into a virtual
    # positive-forward Ackermann problem, then maps the command back through a
    # final reverse-only velocity and curvature guard.
    ReverseHandoff:
      plugin: "smartcar_nav2::ReverseOnlyMPPIController"
      # Keep the reverse terminal rollout inside the C1 corner rather than
      # projecting beyond the strict handoff pose into the outer keepout.
      time_steps: 60
      model_dt: 0.05
      batch_size: 1000
      iteration_count: 2
      vx_std: 0.035
      vy_std: 0.0
      wz_std: 0.12
      vx_max: 0.09
      vx_min: 0.02
      vy_max: 0.0
      wz_max: 0.20
      prune_distance: 1.0
      transform_tolerance: 0.1
      temperature: 0.2
      gamma: 0.015
      motion_model: "Ackermann"
      visualize: false
      retry_attempt_limit: 1
      reset_period: 1.0
      regenerate_noises: false
      AckermannConstraints:
        min_turning_r: 0.55
      critics:
        - ConstraintCritic
        - CostCritic
        - GoalCritic
        - GoalAngleCritic
        - PathAlignCritic
        - PathFollowCritic
        - PathAngleCritic
      ConstraintCritic:
        enabled: true
        cost_power: 1
        cost_weight: 16.0
      CostCritic:
        enabled: true
        cost_power: 1
        cost_weight: 3.81
        critical_cost: 300.0
        consider_footprint: true
        collision_cost: 1000000.0
        near_goal_distance: 0.30
      GoalCritic:
        enabled: true
        cost_power: 1
        cost_weight: 5.0
        threshold_to_consider: 0.70
      GoalAngleCritic:
        enabled: true
        cost_power: 1
        cost_weight: 12.0
        threshold_to_consider: 0.70
      PathAlignCritic:
        enabled: true
        cost_power: 1
        cost_weight: 18.0
        max_path_occupancy_ratio: 0.10
        trajectory_point_step: 2
        threshold_to_consider: 0.30
        offset_from_furthest: 4
        use_path_orientations: true
      PathFollowCritic:
        enabled: true
        cost_power: 1
        cost_weight: 8.0
        offset_from_furthest: 4
        threshold_to_consider: 0.70
      PathAngleCritic:
        enabled: true
        cost_power: 1
        cost_weight: 12.0
        offset_from_furthest: 2
        threshold_to_consider: 0.30
        max_angle_to_furthest: 0.75
        forward_preference: true
    # Dedicated path controller for the one permitted recovery motion. It
    # operates in the virtual-forward frame, while the wrapper's final guard
    # makes the physical command strictly reverse and zero-curvature.
    ReverseRecovery:
      plugin: "smartcar_nav2::ReverseOnlyMPPIController"
      time_steps: 30
      model_dt: 0.05
      batch_size: 400
      iteration_count: 1
      vx_std: 0.015
      vy_std: 0.0
      wz_std: 0.001
      vx_max: 0.05
      vx_min: 0.015
      vy_max: 0.0
      wz_max: 0.0
      prune_distance: 0.30
      transform_tolerance: 0.1
      temperature: 0.2
      gamma: 0.015
      motion_model: "Ackermann"
      visualize: false
      retry_attempt_limit: 1
      reset_period: 1.0
      regenerate_noises: false
      AckermannConstraints:
        min_turning_r: 0.55
      critics:
        - ConstraintCritic
        - CostCritic
        - GoalCritic
        - GoalAngleCritic
        - PathAlignCritic
        - PathFollowCritic
        - PathAngleCritic
      ConstraintCritic:
        enabled: true
        cost_power: 1
        cost_weight: 16.0
      CostCritic:
        enabled: true
        cost_power: 1
        cost_weight: 3.81
        critical_cost: 300.0
        consider_footprint: true
        collision_cost: 1000000.0
        near_goal_distance: 0.15
      GoalCritic:
        enabled: true
        cost_power: 1
        cost_weight: 5.0
        threshold_to_consider: 0.30
      GoalAngleCritic:
        enabled: true
        cost_power: 1
        cost_weight: 12.0
        threshold_to_consider: 0.30
      PathAlignCritic:
        enabled: true
        cost_power: 1
        cost_weight: 18.0
        max_path_occupancy_ratio: 0.10
        trajectory_point_step: 2
        threshold_to_consider: 0.15
        offset_from_furthest: 2
        use_path_orientations: true
      PathFollowCritic:
        enabled: true
        cost_power: 1
        cost_weight: 8.0
        offset_from_furthest: 2
        threshold_to_consider: 0.30
      PathAngleCritic:
        enabled: true
        cost_power: 1
        cost_weight: 12.0
        offset_from_furthest: 1
        threshold_to_consider: 0.15
        max_angle_to_furthest: 0.75
        forward_preference: true

local_costmap:
  local_costmap:
    ros__parameters:
      update_frequency: 10.0
      publish_frequency: 2.0
      global_frame: odom_combined
      robot_base_frame: base_footprint
      rolling_window: true
      width: 3
      height: 3
      resolution: 0.03
      # pi-invariant conservative hull. MPPI evaluates the same physical body
      # at a virtual yaw shifted by pi, so an asymmetric rear-axle footprint
      # would swap its front and rear collision extents.
      footprint: "[[0.27, 0.13], [0.27, -0.13], [-0.27, -0.13], [-0.27, 0.13]]"
      footprint_padding: 0.03
      plugins: ["obstacle_layer", "inflation_layer"]
      obstacle_layer:
        plugin: "nav2_costmap_2d::ObstacleLayer"
        enabled: true
        observation_sources: scan
        scan:
          topic: /scan
          data_type: LaserScan
          clearing: true
          marking: true
          obstacle_max_range: 2.5
          obstacle_min_range: 0.25
          raytrace_max_range: 3.0
          raytrace_min_range: 0.0
      inflation_layer:
        plugin: "nav2_costmap_2d::InflationLayer"
        enabled: true
        inflation_radius: 0.55
        cost_scaling_factor: 5.0
      always_send_full_costmap: true

global_costmap:
  global_costmap:
    ros__parameters:
      update_frequency: 5.0
      publish_frequency: 1.0
      global_frame: odom_combined
      robot_base_frame: base_footprint
      rolling_window: true
      width: 10
      height: 10
      resolution: 0.05
      # pi-invariant hull of the real footprint. This keeps virtual-yaw
      # reverse planning collision-equivalent to the physical vehicle.
      footprint: "[[0.27, 0.13], [0.27, -0.13], [-0.27, -0.13], [-0.27, 0.13]]"
      footprint_padding: 0.03
      plugins: ["obstacle_layer", "inflation_layer"]
      obstacle_layer:
        plugin: "nav2_costmap_2d::ObstacleLayer"
        enabled: true
        observation_sources: scan
        scan:
          topic: /scan
          data_type: LaserScan
          clearing: true
          marking: true
          obstacle_max_range: 2.5
          obstacle_min_range: 0.25
          raytrace_max_range: 3.0
          raytrace_min_range: 0.0
      inflation_layer:
        plugin: "nav2_costmap_2d::InflationLayer"
        enabled: true
        inflation_radius: 0.65
        cost_scaling_factor: 5.0
      always_send_full_costmap: true

planner_server:
  ros__parameters:
    use_sim_time: false
    expected_planner_frequency: 5.0
    planner_plugins: ["GridBased"]
    GridBased:
      plugin: "nav2_smac_planner/SmacPlannerHybrid"
      downsample_costmap: false
      downsampling_factor: 1
      # Semantic waypoints, especially the QR direction handoff, must not be
      # replaced by a nearby planner endpoint. Fail and replan if exact is blocked.
      tolerance: 0.0
      allow_unknown: true
      max_iterations: 100000
      max_on_approach_iterations: 1000
      # The free-heading BT has a 2.4 s action-level budget. Bound one
      # underlying Hybrid-A* search below it so a rejected heading cannot
      # stall the next route attempt for multiple seconds.
      max_planning_time: 1.0
      # Forward-only search. Reverse paths are generated by rotating the
      # vehicle headings by pi in the reverse BT node, then restoring them.
      motion_model_for_search: "DUBIN"
      angle_quantization_bins: 72
      analytic_expansion_ratio: 3.5
      analytic_expansion_max_length: 3.0
      minimum_turning_radius: 0.55
      non_straight_penalty: 1.2
      cost_penalty: 2.0
      retrospective_penalty: 0.015
      lookup_table_size: 20.0
      cache_obstacle_heuristic: false
      # Humble's Smac smoother is collision-aware but does not preserve the
      # Hybrid-A* curvature bound. Keep the DUBIN lattice path intact so a
      # valid R=0.55 m plan cannot be tightened before reverse validation.
      smooth_path: false

velocity_smoother:
  ros__parameters:
    use_sim_time: false
    smoothing_frequency: 20.0
    # Preserve the Ackermann curvature cone while acceleration limits ramp the
    # command before the downstream direction guard.
    scale_velocities: true
    feedback: "OPEN_LOOP"
    max_velocity: [0.15, 0.0, 0.75]
    min_velocity: [-0.15, 0.0, -0.75]
    max_accel: [0.5, 0.0, 1.0]
    max_decel: [-2.0, 0.0, -2.0]
    odom_topic: "odom_combined"
    odom_duration: 0.1
    deadband_velocity: [0.0, 0.0, 0.0]
    velocity_timeout: 0.10
